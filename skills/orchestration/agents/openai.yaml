interface:
  display_name: "Orchestrator"
  short_description: "Initialize this session as an orchestrator: one senior advisor lane, cheap workers"
  default_prompt: "Use $orchestration to initialize this session as the orchestrator. Ask me which advisor lane to use per the skill's routing table, then route all judgment through it and all execution to the cheapest capable worker lane."
