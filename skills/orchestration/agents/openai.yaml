# Codex skill-interface metadata.
interface:
  display_name: "Orchestrator"
  short_description: "Keep judgment in the main session and delegate information gathering and execution"
  default_prompt: "Use $orchestration to apply the universal orchestrator-worker split to this session."
